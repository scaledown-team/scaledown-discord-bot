version: 1.0

runtime: python311

build:
  commands:
    build:
      - pip install -r discord-bot/requirements.txt

run:
  command: cd discord-bot && python bot.py
  network:
    port: 8080
    env: PORT
  env:
    - name: DOCS_PATH
      value: "/app/source"
    - name: PORT
      value: "8080"
